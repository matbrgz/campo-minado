<Page
    x:Class="MinesweeperUno.ConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinesweeperUno"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
        <TextBlock Text="Game Configuration" FontSize="48" HorizontalAlignment="Center"/>

        <StackPanel>
            <TextBlock Text="Width" />
            <Slider x:Name="WidthSlider" Minimum="5" Maximum="20" Value="9" TickFrequency="1" TickPlacement="BottomRight"/>
            <TextBlock Text="{x:Bind WidthSlider.Value, Mode=OneWay}" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Height" />
            <Slider x:Name="HeightSlider" Minimum="5" Maximum="20" Value="9" TickFrequency="1" TickPlacement="BottomRight"/>
            <TextBlock Text="{x:Bind HeightSlider.Value, Mode=OneWay}" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel>
            <TextBlock Text="Bombs" />
            <Slider x:Name="BombsSlider" Minimum="1" Maximum="100" Value="10" TickFrequency="1" TickPlacement="BottomRight"/>
            <TextBlock Text="{x:Bind BombsSlider.Value, Mode=OneWay}" HorizontalAlignment="Center"/>
        </StackPanel>

        <Button Content="Start Game" Click="StartGameButton_Click" HorizontalAlignment="Center"/>
    </StackPanel>
</Page>
